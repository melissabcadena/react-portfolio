import React from 'react';
import Nav from '../Nav'

function Header () {
    return (
        <div>
            <Nav />
            <div class="hero-image">
            </div>
        </div>
    )
}

export default Header;